<form method="post" action="{{ path('app_offredemploi_delete', {'id': offredemploi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offredemploi.id) }}">
    <button class="btn">Delete</button>
</form>
